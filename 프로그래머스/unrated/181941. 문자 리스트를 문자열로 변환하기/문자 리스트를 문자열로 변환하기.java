class Solution {
    public String solution(String[] arr) {
        
        int arr_length = arr.length;
        String answer = "";
        
        for(int i=0; i<arr_length; i++) {
            answer += arr[i];
        }
        
        return answer;
    }
}